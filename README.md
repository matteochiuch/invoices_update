The program performs a series of API calls to retrieve a JSON-formatted dataframe containing multiple data types, including nested structures. It then parses and transforms the data to ensure consistency and usability for further processing.

A clean and standardized dataframe is created by selecting the relevant fields and exported to Google Sheets, where columns are renamed, data is standardized, concatenated, and continuously updated.
